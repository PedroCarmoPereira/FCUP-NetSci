# NSC HW 1 Part 3

For the 3rd part of the NetSci Homework.

## Structure

## Requirements

* Exercise 7:

    + `pip install -r requirements.txt`

    + `python er.py`, generates files `random1.txt` and `random2.txt`, calculates giant component, runs experiment and prints plot.
